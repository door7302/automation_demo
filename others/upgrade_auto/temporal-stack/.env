# Image versions for the Temporal stack. Bump these to pin specific releases;
# `latest` is used by default via docker-compose.yml if left unset.
POSTGRESQL_VERSION=16
TEMPORAL_VERSION=latest
TEMPORAL_UI_VERSION=latest
TEMPORAL_ADMINTOOLS_VERSION=latest
