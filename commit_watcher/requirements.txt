# Pinned to the verified-good set (tested on Python 3.10.12). Use
# requirements.lock for a fully reproducible install incl. transitive deps.
motor==3.7.1
PyYAML==6.0.3
pygnmi==0.8.15
fastapi==0.139.0
uvicorn==0.51.0
junos-eznc==2.8.2
lxml==6.1.1
